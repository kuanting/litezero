#!/usr/bin/env bash
# Pre-commit hook for the LiteZero reference simulator.
# Enable once per clone with:  git config core.hooksPath .githooks
set -euo pipefail
cd "$(git rev-parse --show-toplevel)"

echo "[pre-commit] dead-secret lint"
node --experimental-transform-types --no-warnings scripts/dead-secret-lint.ts
